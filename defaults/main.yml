---
# Default Variables for the role.
# Actually these are surely overridden by the tasks of the role
# The purpose is indeed to create a set of variables based on project name

base_dir: "/vagrant"
project_name: "topixradio"
index_folder: "{{base_dir}}/public/topixradio"